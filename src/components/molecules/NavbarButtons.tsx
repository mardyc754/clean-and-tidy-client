import { NavbarButton, Button } from '../atoms/buttons';

const NavbarButtons = () => {
  return (
    <>
      <NavbarButton
        name="For home"
        onClick={() => {
          /** TODO */
        }}
      />
      <NavbarButton
        name="For office"
        onClick={() => {
          /** TODO */
        }}
      />
      <NavbarButton
        name="Registration"
        onClick={() => {
          /** TODO */
        }}
      />
      <Button
        name="Login"
        onClick={() => {
          /** TODO */
        }}
      />
    </>
  );
};

export default NavbarButtons;
