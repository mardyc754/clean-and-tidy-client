import { DialogTriggerButton } from '.';

const ManageEmployeeDialog = () => {
  return (
    <DialogTriggerButton dialogTitle="Manage employee" buttonLabel="Manage">
      test
    </DialogTriggerButton>
  );
};

export default ManageEmployeeDialog;
